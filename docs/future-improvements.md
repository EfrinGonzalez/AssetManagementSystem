# Future Improvements

- Replace in-memory repositories with SQLite/PostgreSQL.
- Add queue provider (Service Bus) and retry policies (Polly).
- Add authN/authZ with Azure AD and JWT bearer tokens.
- Add distributed attribution compute for large universes.
