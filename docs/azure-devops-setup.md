# Azure DevOps Setup

1. Create Project and import repository.
2. Create service connections (Azure RM + ACR).
3. Configure variable groups for environment config.
4. Enable pipeline from `azure-pipelines.yml`.
5. Add branch policies (build validation, reviewers).
